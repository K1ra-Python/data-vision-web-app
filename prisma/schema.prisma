generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // указываем тип базы
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}
